import React from 'react'


const Preview = () => {
  
  return(
      <div>
        <h1>fff</h1>
        {/* <button className='but1' onClick={()=>setname1(true)}>Use to See Functional Component</button>
        <button className='but2' onClick={()=>setname2(true)}>Use to See Component Component</button> */}
    </div>
  )
  // {name1&&<Functional/>}
  // {name2&&<Classcomponents/>}
  }
   export default Preview;